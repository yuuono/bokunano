# 意味ASTの分割方針

## 目的

現在作成済みの12,720件の意味ASTを、後から日本語指示やPythonコードの表記違いが訓練側とテスト側に混入しないよう、意味ASTの段階で分割する。

この文書では分割方針と実行結果を定める。組合せ汎化テスト用の670件と反復ASTの1,152件は、それぞれ独立したJSONLとして抽出・生成済みである。残る12,050件も訓練、検証、通常テストへ分割済みである。日本語指示とPythonコードの生成は行わない。

## 前提となる意味AST

現在の12,720件は、24個の単独操作から、同じ操作を繰り返さず、順序を区別して1〜3個を並べたものである。

| 操作数 | 件数 |
|---|---:|
| 1操作 | 24 |
| 2操作 | 552 |
| 3操作 | 12,144 |
| 合計 | 12,720 |

## test_suite

`test_suite`は次の5種類に統一する。

```yaml
test_suite: normal | paraphrase | compositional | boundary | repetition
```

元の課題で要求される4種類は`normal`、`paraphrase`、`compositional`、`boundary`である。`repetition`は、同一操作が隣接するASTを評価するために追加した独立テストである。

## テスト集合は別々の軸として扱う

通常テストを基準とし、境界値テストでは実行入力だけを変更する。言い換えテストは複合操作を扱わず、訓練済みの24単独操作に未学習表現を割り当てる独立評価とする。組合せ汎化テストでは意味ASTだけを変更する。

| test_suite | 意味AST | 日本語表現 | 実行入力 |
|---|---|---|---|
| `normal` | 訓練に含まれない通常テスト用AST | 訓練用表現辞書 | hidden test入力 |
| `paraphrase` | 24個の単独操作AST | テスト専用30表現 | 単独操作用hidden test入力 |
| `compositional` | 選択した5操作対に基づく670件 | 訓練用表現辞書 | hidden test入力 |
| `boundary` | `normal`と同じAST | 訓練用表現辞書 | 境界値入力 |
| `repetition` | 同一操作が隣接する1,152件 | 未決定 | 未決定 |

`normal`と`boundary`で同じ意味ASTを使うのは意図的である。2集合の差を実行入力だけにすることで、通常テストからの性能低下が境界値入力によるものかを切り分ける。

`paraphrase`は、訓練へ割り当てた24個の単独操作の意味を参照し、採用した30表現から1操作1文を作る。train、validation、normalのAST分割数は消費しない。`boundary`は`normal`のASTを再利用する。`compositional`は12,720件から670件を使用する。`repetition`は現在の12,720件には含まれず、別に1,152件を生成済みである。

## 組合せ汎化テストを先に確保する

組合せ汎化テストには、次の5個の操作対を使用する。

| 操作対ID | 操作A | 操作B |
|---|---|---|
| `pair-01` | 偶数だけを残す抽出 | 降順の並べ替え |
| `pair-02` | 奇数だけを残す抽出 | `k`を加える変換 |
| `pair-03` | `k`以上を残す抽出 | 末尾`k`個の切り出し |
| `pair-04` | 2倍する変換 | 現在順を反転する並べ替え |
| `pair-05` | 絶対値を取る変換 | 先頭から1個おきの切り出し |

各操作対について、2操作の順序違い2件と、その2操作を両方含む3操作ASTを訓練から除外して`compositional`へ割り当てる。

| 操作数 | compositionalへ割り当てる件数 |
|---|---:|
| 2操作 | 10 |
| 3操作 | 660 |
| 合計 | 670 |

操作の順序を区別しているため、2操作では`A → B`と`B → A`を両方とも除外する。3操作では、異なる3操作からなる6通りの順序をすべて除外する。

この詳細は[組合せ汎化テストの方針](compositional_generalization_test_policy.md)で定める。

670件は`scripts/semantic_asts/extract_compositional_semantic_asts.py`で既存の12,720件から抽出し、`data/semantic_asts/compositional_semantic_asts.jsonl`に保存済みである。

## 組合せ汎化用670件を確保した後の件数

12,720件から`compositional`用の670件を確保すると、残りは12,050件になる。

| 操作数 | 元の件数 | compositional | 残り |
|---|---:|---:|---:|
| 1操作 | 24 | 0 | 24 |
| 2操作 | 552 | 10 | 542 |
| 3操作 | 12,144 | 660 | 11,484 |
| 合計 | 12,720 | 670 | 12,050 |

この12,050件が、訓練、検証、通常テストへ割り当てる候補になる。

## 1操作ASTの扱い

24個の1操作ASTは、すべて訓練へ割り当てる方針とする。

組合せ汎化テストは、個々の操作を単独では訓練で学習していることを前提に、未知の組み合わせを作れるか評価する。1操作ASTの一部を通常テストへ回すと、失敗原因が「操作自体を学習していないため」か「操作を組み合わせられないため」か分からなくなる。

したがって、検証と通常テストは2操作ASTと3操作ASTから作る。

## 残り12,050件の分割件数

分割件数は次のとおりである。

| 操作数 | 母数 | 訓練 | 検証 | 通常テスト |
|---|---:|---:|---:|---:|
| 1操作 | 24 | 24 | 0 | 0 |
| 2操作 | 542 | 434 | 54 | 54 |
| 3操作 | 11,484 | 9,188 | 1,148 | 1,148 |
| 合計 | 12,050 | 9,646 | 1,202 | 1,202 |

12,050件に対して訓練約80.0%、検証約10.0%、通常テスト約10.0%になる。1操作ASTをすべて訓練へ入れるため、操作数ごとに一律80対10対10にはならない。

検証と通常テストへ割り当てるASTは重複させない。`boundary`は、分割後の`normal`用1,202 ASTを再利用する。`paraphrase`はこの分割とは別に、24個の単独操作ASTを使う。

## 採用した割り当て方式

案Bの「ハッシュ順に並べて指定件数で切る」を採用する。実装は`scripts/semantic_asts/split_semantic_asts.py`とする。

ハッシュと割り当ては次の手順で決定する。

1. `data/semantic_asts/combined_semantic_asts.jsonl`の12,720件から、`data/semantic_asts/compositional_semantic_asts.jsonl`の670件を`spec_id`と意味ASTの両方が一致することを確認して除く。
2. 意味ASTを、JSONの辞書キーを昇順、区切り文字を`,`と`:`、日本語をそのまま保持する形式で正規化する。
3. 正規化したUTF-8文字列のSHA-256を計算する。
4. 残った12,050件を1操作、2操作、3操作に分け、それぞれをSHA-256の16進文字列の昇順に並べる。同じハッシュになった場合の順序は正規化文字列の昇順で決める。
5. 各操作数の列を次の位置で切り分ける。

| 操作数 | train | val | normal |
|---|---|---|---|
| 1操作 | 先頭24件 | なし | なし |
| 2操作 | 先頭434件 | 次の54件 | 最後の54件 |
| 3操作 | 先頭9,188件 | 次の1,148件 | 最後の1,148件 |

同じ入力ファイルから再実行すれば、同じ分割結果になる。入力ASTを後から追加・削除・変更した場合は、境界付近の既存ASTの割り当てが変わる可能性があるため、分割コードを再実行して各検査をやり直す。

## 分割の実行結果

2026年9月17日に`scripts/semantic_asts/split_semantic_asts.py`を実行し、次の3ファイルを生成した。

| 分類 | 出力ファイル | 件数 | `split` | `test_suite` |
|---|---|---:|---|---|
| 訓練 | `data/semantic_asts/train_semantic_asts.jsonl` | 9,646 | `train` | `null` |
| 検証 | `data/semantic_asts/val_semantic_asts.jsonl` | 1,202 | `val` | `null` |
| 通常テスト | `data/semantic_asts/normal_semantic_asts.jsonl` | 1,202 | `test` | `normal` |
| 合計 |  | 12,050 |  |  |

出力では、抽出元の`spec_id`と`semantic_ast`を変更せず、`split`と`test_suite`を追加した。`normal`を再利用する`boundary`の派生レコードと、単独操作ASTを使う`paraphrase`レコードは、この分割処理では生成しない。

再生成した3ファイルのSHA-256は次のとおりである。

| 出力ファイル | SHA-256 |
|---|---|
| `data/semantic_asts/train_semantic_asts.jsonl` | `e4e94207820d852f9da8cd4962b7ca24dec4be892d14731cd0d1d517822e3d38` |
| `data/semantic_asts/val_semantic_asts.jsonl` | `95adb06d425b00585782cd1a904d1c405f8642ae5c51f1e9a11e0621a4e83d8f` |
| `data/semantic_asts/normal_semantic_asts.jsonl` | `69a2d5718a08ff823a0827f6d6e9cd4c644f57fd2fa1d22e66994a8f988123c9` |

保存前に、次を機械的に確認した。

- 入力が12,720件、組合せ汎化用が670件である
- 組合せ汎化用670件の`spec_id`と意味ASTが抽出元に一致する
- 除外後が1操作24件、2操作542件、3操作11,484件である
- 分割結果が訓練9,646件、検証1,202件、通常テスト1,202件である
- 3集合の`spec_id`と正規化意味ASTがそれぞれ重複せず、集合間にも重複がない
- 3集合の和集合が、組合せ汎化用670件を除いた12,050件と完全に一致する
- 24個すべての1操作ASTが訓練に含まれる
- 組合せ汎化用の5ペアが訓練AST内で共起していない

### 操作別の出現頻度

各セルは、その分類・操作数の意味ASTに該当操作が出現した回数を表す。検証と通常テストには1操作ASTを割り当てないため、1操作の列は訓練だけを示す。

| atomic ID | 操作 | train 1 | train 2 | train 3 | val 2 | val 3 | normal 2 | normal 3 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| `atomic-000001` | `{"filter":["even"]}` | 1 | 36 | 1,103 | 4 | 129 | 4 | 130 |
| `atomic-000002` | `{"filter":["odd"]}` | 1 | 37 | 1,095 | 4 | 134 | 3 | 133 |
| `atomic-000003` | `{"filter":["gt_k"]}` | 1 | 40 | 1,206 | 3 | 153 | 3 | 129 |
| `atomic-000004` | `{"filter":["ge_k"]}` | 1 | 37 | 1,093 | 3 | 141 | 4 | 128 |
| `atomic-000005` | `{"filter":["lt_k"]}` | 1 | 35 | 1,178 | 6 | 156 | 5 | 154 |
| `atomic-000006` | `{"filter":["le_k"]}` | 1 | 35 | 1,208 | 6 | 145 | 5 | 135 |
| `atomic-000007` | `{"filter":["multiple_of_k"]}` | 1 | 40 | 1,163 | 4 | 158 | 2 | 167 |
| `atomic-000008` | `{"filter":["positive"]}` | 1 | 37 | 1,210 | 3 | 126 | 6 | 152 |
| `atomic-000009` | `{"filter":["negative"]}` | 1 | 38 | 1,176 | 4 | 147 | 4 | 165 |
| `atomic-000010` | `{"filter":["zero"]}` | 1 | 36 | 1,182 | 5 | 153 | 5 | 153 |
| `atomic-000011` | `{"map":["add_k"]}` | 1 | 34 | 1,072 | 6 | 158 | 4 | 132 |
| `atomic-000012` | `{"map":["sub_k"]}` | 1 | 37 | 1,202 | 7 | 144 | 2 | 142 |
| `atomic-000013` | `{"map":["mul_k"]}` | 1 | 32 | 1,178 | 9 | 160 | 5 | 150 |
| `atomic-000014` | `{"map":["mul_const",2]}` | 1 | 37 | 1,088 | 2 | 153 | 5 | 121 |
| `atomic-000015` | `{"map":["mul_const",3]}` | 1 | 32 | 1,206 | 6 | 141 | 8 | 141 |
| `atomic-000016` | `{"map":["negate"]}` | 1 | 34 | 1,176 | 4 | 152 | 8 | 160 |
| `atomic-000017` | `{"map":["abs"]}` | 1 | 32 | 1,100 | 6 | 128 | 6 | 134 |
| `atomic-000018` | `{"map":["square"]}` | 1 | 39 | 1,214 | 4 | 127 | 3 | 147 |
| `atomic-000019` | `{"order":"ascending"}` | 1 | 38 | 1,180 | 6 | 156 | 2 | 152 |
| `atomic-000020` | `{"order":"descending"}` | 1 | 34 | 1,061 | 3 | 151 | 7 | 150 |
| `atomic-000021` | `{"order":"reverse"}` | 1 | 40 | 1,103 | 1 | 127 | 3 | 132 |
| `atomic-000022` | `{"slice":["take_first_k"]}` | 1 | 40 | 1,214 | 2 | 121 | 4 | 153 |
| `atomic-000023` | `{"slice":["take_last_k"]}` | 1 | 37 | 1,078 | 3 | 142 | 4 | 142 |
| `atomic-000024` | `{"slice":["every_other"]}` | 1 | 31 | 1,078 | 7 | 142 | 6 | 142 |

## 各集合の生成方針

### train

- 訓練へ割り当てた意味ASTを使用する
- 訓練用表現辞書を使用する
- build入力で生成コードを検証する

### val

- 検証へ割り当てた意味ASTを使用する
- 訓練用表現辞書を使用する
- build入力で生成コードを検証する
- validation lossの監視に使用する

### normal

- 通常テストへ割り当てた意味ASTを使用する
- 訓練用表現辞書を使用する
- hidden test入力で評価する

### paraphrase

- 24個の単独操作ASTだけを使用し、2操作・3操作ASTは使用しない
- テスト専用の採用30表現を1件ずつ対応する単独操作ASTへ割り当てる
- 表現1件につき1操作の全文指示を1文作り、合計30文とする
- 単独操作用hidden test入力で評価する
- 操作の意味は訓練済みとし、未学習の日本語表現を理解できるかを測る

### compositional

- 5個の操作対から定めた670件を使用する
- 訓練用表現辞書を使用する
- hidden test入力で評価する
- テスト専用表現辞書は使用しない

### boundary

- `normal`と同じ意味ASTを使用する
- 訓練用表現辞書を使用する
- 空リスト、1要素、すべて同値、負数のみなどの境界値入力で評価する
- 抽出を含む意味ASTでは、非空入力の全要素が除外される「すべて不合格」の入力を作る
- 抽出を含まない意味ASTでは、「すべて不合格」は適用対象外とする

### repetition

- 同一操作が隣接する2操作24件、3操作1,128件の合計1,152件を使用する
- 現在の12,720件とは別に`data/semantic_asts/repetition_semantic_asts.jsonl`へ生成済み
- 日本語表現と実行入力の方針はまだ決定していない

反復テストの詳細は[反復汎化テストの方針](repetition_generalization_test_policy.md)で定める。

## コード生成後の完全重複除外

参照タスクでの確認に基づき、意味ASTの入出力関係が同じという理由だけで、意味AST全体を事前に除外しない。`compositional`は決定済みの670件のままとする。

訓練側と`compositional`側の意味ASTから複数の等価コードを生成し、構文、安全性、シグネチャ、実行時間、参照インタプリタとの一致を検証する。その後、次の完全重複だけをレコード単位で除外する。

- `compositional`側の正解Pythonコードと文字列全体が完全一致した訓練レコード
- 訓練データ内の別レコードとPythonコード文字列全体が完全一致したレコード

同じ入出力関係でも、Pythonコード文字列が完全一致しなければ、それだけを理由に訓練候補から除外しない。完全重複の除外後は、訓練コードと`compositional`正解コードの積集合が空であることを機械確認する。

この処理で減るのはコード生成後の訓練レコードであり、事前に決めた意味ASTの分割数ではない。詳細は[生成コードの完全重複を訓練データから除外する方針](code_duplicate_exclusion_policy.md)で定める。

## データレコードの管理項目

少なくとも次の項目で、分割、テスト集合、日本語辞書、実行入力を区別する。

```yaml
split:      train | val | test
test_suite: normal | paraphrase | compositional | boundary | repetition
dictionary: train | test_only
input_set:  build | hidden | boundary
```

`train`と`val`では`test_suite`を空欄または`null`にする。`test`では5種類のいずれかを設定する。

`normal`と`boundary`は同じ意味ASTを使うため、同じ元問題から派生したレコードであることを追跡できる`family_id`を持たせる。`paraphrase`にも単独操作ASTの`semantic_hash`から計算した`family_id`を付けるが、normal用2・3操作ASTとの対応は要求しない。IDの計算方法と最終レコードの項目は、[データレコードの作成・管理方針](data_record_policy.md)で定める。

## 分割後の確認

分割後に、少なくとも次を機械的に確認する。

1. 訓練用の正規化意味ASTが、検証用または通常テスト用の意味ASTと完全一致していない
2. `normal`と`boundary`が同じ意味ASTを共有している
3. `paraphrase`が30件の1操作文だけで構成され、24操作を覆っている
4. テスト専用表現辞書のエントリが、訓練用指示文に使われていない
5. `compositional`用の5操作対が、訓練用意味AST内で共起していない
6. 24個すべての単独操作が、訓練用の1操作ASTとして存在する
7. 各操作の出現頻度を操作数別、split別に集計する

コード生成と完全重複除外の後には、さらに次を確認する。

1. 訓練コードと`compositional`正解コードに完全一致がない
2. 訓練データ内に完全に同じコードが複数残っていない
3. 除外した各レコードについて、理由、コードハッシュ、対応するテストIDが記録されている

## 現時点で確定していないもの

- `repetition`で使用する日本語表現辞書
- `repetition`で使用する実行入力集合

これらは該当するデータ生成を始める前に決定する。
