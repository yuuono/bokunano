# 夏休み実習課題：Boku-nanoの開発

## 課題名

**日本語の指示から限定的なPython関数を生成するSmall Language Modelのフルスクラッチ学習**

本課題では、極めて限定されたPythonプログラミング領域を対象として、データ生成、トークナイザ作成、Transformerモデル構築、事前学習、実行評価までを一貫して行う。

対象: 学部4年生 / 期間: 6週間 / 計算資源: GeForce RTX 5090 × 1

必修モデルを「Boku1-nano」、発展課題として作成する約1Bパラメータのモデルを「Boku1 1B」と呼ぶ。

## 課題の目的

本課題の主目的は、高いベンチマーク性能を達成することではない。したがって、到達精度は評価しない。評価するのは、データ生成から学習・評価までのパイプライン全体を自分の手で組み、各段階で何が起きているかを説明できることである。

学生が次の一連の工程を自分で完成させ、「ランダムな重みから学習した言語モデルが、未学習の指示に対して実行可能なプログラムを生成する」ことを体験することが目的なので、ぜひ、自らの手で実装してみてほしい。

- 対象領域を機械的に定義する
- 正解プログラムとテストを自動生成する
- 教師モデルを使って日本語表現を増やす
- 合成データを検証・重複除去・分割する
- トークナイザをゼロから学習する
- Transformerをランダム初期値から学習する
- 生成コードを実際に実行して評価する
- 成功例だけでなく失敗例も分析する

## 「フルスクラッチ学習」の定義

本課題では、次のすべてを満たすものだけをフルスクラッチ学習と呼ぶ。

1. 学生モデルの重みをランダムに初期化する
2. 既存モデルの重み、埋め込み、LoRA、Adapterを使用しない
3. トークナイザも本課題の訓練データだけから作成する
4. 教師モデルはオフラインでのデータ生成にだけ使用する
5. 学習時および評価時に教師モデルのlogitや内部状態を使用しない
6. 評価時には学生モデルだけでプログラムを生成する
7. テストデータの問題、言い換え、正解コードを訓練データへ混入させない

Transformerそのものは、PyTorchやTransformersの既存部品を利用してよい。ただし、学習済みモデルを読み込むのではなく、設定ファイルから新しいモデルを生成しなければならない。

### 将来構想

本課題は、研究室で構想している「Boku1 1B」(合成データのみで学習した日本語・コーディング特化の1Bパラメータ級 Dense モデル)の予備実験にあたる。本課題を完了した時点で、Boku1 の設計に必要な実測データが手元に揃うことになる。

## 全体像

```
Phase 1  データ生成器 + 実行検証     2週
Phase 2  トークナイザ                1週
Phase 3  モデル実装                  1週
Phase 4  学習                        1週
Phase 5  評価                        1週
```

## 対象とする問題

日本語の指示を入力として、次の固定シグネチャを持つPython関数を生成する。

```python
def solve(xs: list[int], k: int) -> list[int]:
    ...
```

入力条件は次のように限定する。

- `xs`は長さ0〜20の整数リスト
- 各要素は原則として−100〜100
- `k`は1〜10
- 出力は整数リスト
- 外部入力、ファイル、ネットワーク、乱数は使用しない
- 副作用を持たない純粋関数とする

## 対象とするプログラミング上の操作

以下から1〜3個を組み合わせた問題だけを扱う。

**抽出**

- 偶数だけを残す
- 奇数だけを残す
- `k`より大きい、以上、小さい、以下の値を残す
- `k`の倍数だけを残す
- 正、負、ゼロの要素を残す

**変換**

- 各要素に`k`を加える、引く、掛ける
- 各要素を2倍、3倍する
- 符号を反転する
- 絶対値を取る
- 二乗する

**並べ替え**

- 昇順
- 降順
- 逆順

**切り出し**

- 先頭から`k`個
- 末尾から`k`個
- 1個おきに取得する

## 問題例

入力指示：

> 整数リストxsからk以上の偶数だけを残し、それぞれを2倍して昇順に並べるsolve関数を書いてください。

期待されるコードの一例：

```python
def solve(xs: list[int], k: int) -> list[int]:
    return sorted([x * 2 for x in xs if x >= k and x % 2 == 0])
```

正解コードとの文字列一致は要求しない。同じ入出力関係を実現していれば正解とする。

## 対象外の要素

以下は本課題では扱わない。

- 任意のPythonプログラミング
- クラス、再帰、非同期処理
- ファイルやネットワークアクセス
- NumPy、Pandasなどの外部ライブラリ
- アルゴリズム競技問題
- 複数関数からなるプログラム
- 自由形式の会話
- HumanEvalなど一般的なコード生成ベンチマークでの高性能化

## なぜここまで狭くするのか？

この狭さが本課題の成立条件である。

1. 語彙が閉じるため、トークナイザを 2,048 語彙まで小さくできる。埋め込みパラメータが減り、モデル全体を小さくできる。
2. **生成されたコードを実際に実行して正誤を機械判定できる。** 評価が完全に自動化され、人手による採点が不要になる。
3. 学習データの全件について、それが正しく動くコードであることを保証できる。

## モデル仕様

|                | **Boku-nano**(必修) | **Boku-mini**(発展) |
| -------------- | ------------------- | ------------------- |
| パラメータ数   | 約1,600万           | 約9,100万           |
| d_model        | 384                 | 768                 |
| 層数           | 8                   | 12                  |
| ヘッド数       | 6                   | 12                  |
| d_ff (SwiGLU)  | 1,024               | 2,048               |
| 語彙数         | 2,048               | 8,192               |
| 文脈長         | 512                 | 1,024               |
| 学習トークン   | 3億から10億         | 20億                |
| 学習時間の目安 | 30分から1.5時間     | 一晩(4から6時間)    |

## 教師モデル

教師には、公式の`Qwen/Qwen3-4B-AWQ`を第一候補とする。同モデルは4-bit AWQ版として公開され、モデルページとLICENSEにはApache License 2.0が示されている。[モデルカード](https://huggingface.co/Qwen/Qwen3-4B-AWQ)、[LICENSE](https://huggingface.co/Qwen/Qwen3-4B-AWQ/blob/main/LICENSE)

今回の日本語表現候補生成では、`/home/ono_yusuke/Qwen3-4B-AWQ`に配置したローカル重みを`local_files_only=true`で使用した。clone先では同じrevisionのモデルを任意の場所へ配置し、生成コマンドの`--model-path`で絶対パスを指定する。実行環境、固定した依存バージョン、sampling設定、720件の生成結果は、[`docs/results/japanese_atomic_expression_generation_results.md`](docs/results/japanese_atomic_expression_generation_results.md)に記録する。

教師モデルは主として次の用途に使用する。

- 日本語指示の言い換え
- 原子的な操作の説明文生成
- 初学者向け解説の生成
- 同じプログラムに対する表現の難易度変更

教師モデルに正解プログラムそのものを決めさせてはいけない。正解の意味構造、コード、テストはルールベースで作成し、教師は自然言語（日本語）表現を増やす役割に限定する。

教師生成時には以下を固定・記録する。

- モデル名
- モデルのrevisionまたはコミットID
- 量子化方式
- 推論ライブラリのバージョン
- system prompt
- sampling設定
- seed
- 生成日時
- プロンプトのハッシュ値

思考過程を大量に保存する必要はない。非thinkingモードを使用し、最終的な指示文または解説文だけを保存する。

## データ生成

### 意味表現の生成

自然言語より先に、問題の意味を表す中間表現を生成する。

例：

```json
{
  "filter": ["even", "ge_k"],
  "map": ["mul_const", 2],
  "order": "ascending"
}
```

この中間表現をDSLまたは意味ASTと呼ぶ。

各意味ASTから、独立した二つのプログラムを作る。このプログラムは、Codexに作らせて良い。

- 正解を計算する参照インタプリタ
- 学習対象となるPythonコードを出力するコード生成器

両者の出力をランダムテストで比較する。これにより、コード生成器自身のバグを検出する。

### 日本語指示の生成

次の二段階方式を推奨する。

1. Qwen3にそれぞれのアトミックな操作について10〜30種類の日本語表現を生成させる
2. 人間が、生成させたものに意味の変化がないか確認し、承認済み表現辞書を作る
3. ルールベースの文生成器が、意味ASTと承認済み表現を組み合わせる
4. 一部についてのみ、教師モデルに文全体の言い換えを行わせる

例えば「偶数だけを残す」について、次の表現を用意する。

- 偶数の要素だけを抽出する
- 2で割り切れる値のみを残す
- 奇数を取り除く
- 偶数に該当する要素を選ぶ

教師が生成した文には、「以上」が「より大きい」に変わるなど、意味を変える言い換えがあるため、そのまま無条件で採用するべきではない。人間のチェックを必ず入れること。

旧方式の候補生成では、簡体字・中国語の混入や比較境界の意味変更が確認された。現在は各候補に終止形と接続形を持たせ、最終指示を自然な一文としてルール結合する。旧方式の問題例と今回の生成パラメータは、[`単純操作の日本語表現候補生成結果`](docs/results/japanese_atomic_expression_generation_results.md#旧方式で人間確認から見つかった問題)に記録する。

### コードの構造的変換

同じ意味ASTから複数の等価コードを生成する。この生成器もcodexで生成して良い。

- 内包表記と通常の`for`ループ
- 一時変数の有無
- 変数名の変更
- 条件式の順序変更
- `reverse=True`と逆順操作
- 1行の`return`と複数行形式
- コメントおよび型注釈の有無

すべてのコードを実行し、参照インタプリタと同じ結果になる場合だけ採用する。

### データレコード

最低限、次の情報をJSONLなどで保存する。ただし、必ずしもJSONのスキーマを踏襲する必要はない。中身は各学生の判断に任せる。

```json
{
  "spec_id": "task-000001",
  "semantic_ast": {},
  "instruction_ja": "...",
  "reference_code": "...",
  "tests": [],
  "difficulty": 2,
  "code_style": "list_comprehension",
  "semantic_hash": "...",
  "text_hash": "...",
  "teacher_model": "...",
  "teacher_revision": "...",
  "prompt_hash": "...",
  "verification": {
    "syntax_ok": true,
    "ast_safe": true,
    "tests_passed": true
  }
}
```

## データ規模

夏休み課題では次を目安とする。

| 段階                             |                目安 |
| -------------------------------- | ------------------: |
| 教師モデルによる直接生成         |      0.5〜3M tokens |
| 生成する意味AST                  | 30,000〜100,000種類 |
| 構造変換後の生コーパス           |     50〜200M tokens |
| 検証・選抜後の訓練コーパス       |     30〜100M tokens |
| 学習中にモデルが見る総トークン数 |    100〜300M tokens |
| コンテキスト長                   |          256 tokens |

同じデータを複数epoch使用してよい。ただし、「固有トークン数」と「学習中に見た延べトークン数」を分けて報告すること。

## データの検証と選抜

次の条件をすべて通過したレコードだけを訓練候補とする。

1. `ast.parse`に成功する
2. 許可されていない構文を含まない
3. import、属性アクセス、ファイル操作などを含まない
4. 制限時間内に実行できる
5. 参照インタプリタと全テストで一致する
6. 指定シグネチャを守る
7. 長さ上限を超えない
8. 完全重複コードでない
9. 同じ意味ASTにデータが偏りすぎていない

選抜では、複雑な品質モデルを作る必要はない。以下の単純な均等抽出でよい。

- 操作数1、2、3を均等に近づける
- 各演算子の出現頻度を均す
- コード形式を均す
- 日本語テンプレートを均す
- 一つの意味ASTにつき最大20例などの上限を設ける

## 訓練・検証・テスト分割

データは日本語文やコードを生成する前に、意味AST単位で分割する。

推奨構成は次のとおりである。

- 訓練：80%
- 検証：10%
- テスト：10%

加えて、次のテスト集合を作る。

**通常テスト**

訓練と同じ演算の組み合わせだが、指示文と定数が異なる。

**言い換えテスト**

訓練に使用しなかった日本語テンプレートを使用する。

**組合せ汎化テスト**

例えば、訓練では「偶数抽出」と「降順整列」を別々に見せ、テストでは両者を組み合わせる。

**境界値テスト**

空リスト、要素1個、すべて不合格、すべて同値、負数を含むリストなどを使用する。

意味ASTを分割した後で言い換えやコード変換を行わないと、同じ問題の表記違いが訓練とテストに混入するため注意する。

## トークナイザ

訓練データだけを用いて、BPEまたはUnigramトークナイザを作成する。

推奨設定：

- 語彙数：2,048
- byte fallback：有効
- 日本語とPythonコードを同一語彙で扱う
- 特殊トークンを明示する

例：

```text
<|pad|>
<|bos|>
<|eos|>
<|unk|>
<|task|>
<|code|>
<|explanation|>
```

## 必修モデル：Boku1-nano

推奨構成は約70MパラメータのDecoder-only Transformerである。

| 項目             |       推奨値 |
| ---------------- | -----------: |
| 層数             |           10 |
| hidden size      |          768 |
| Attention head数 |           12 |
| KV head数        |            4 |
| FFN size         |        2,048 |
| 語彙数           |        2,048 |
| 最大系列長       |          256 |
| 位置表現         |         RoPE |
| 正規化           |      RMSNorm |
| 活性化           |       SwiGLU |
| 入出力埋め込み   | weight tying |

ライブラリの都合に合わせて多少変更してよいが、総パラメータ数を報告する。

### 推奨学習設定

- 数値形式：BF16
- Optimizer：AdamW
- 最大learning rate：`3e-4`
- warmup：全stepの3%
- schedule：cosine decay
- weight decay：`0.1`
- gradient clipping：`1.0`
- global batch：32,768 tokens前後
- 学習量：100〜300M tokens
- 評価間隔：100〜250 step
- checkpoint：500 stepごと
- generation評価：temperature 0、最大192 tokens

例えば系列長256、micro batch 8、gradient accumulation 16なら、global batchは32,768 tokensになる。日本語指示からコードを生成する訓練例では、原則として`<|code|>`以降の出力部分だけを損失関数の計算対象とする。日本語教材、コード解説、通常のPythonテキストなど、文章全体を言語モデルとして学習させるデータでは、原則として全トークンを計算対象とする。

例えば、訓練データが次の形だとする。

```text
<|task|>
整数リストの各要素を2倍してください。
<|code|>
def solve(xs, k):
    return [x * 2 for x in xs]
<|eos|>
```

言語モデルは、各位置で「次のトークンは何か」を予測し、その予測が正解からどれだけ外れたかをCross Entropy Lossで測る。ここで、 `<|code|>`以降だけを対象にする場合、損失計算のイメージは次のようになる。

```text
<|task|> 整数リストの各要素を2倍してください。 <|code|> def solve ...
   無視       無視          無視                  計算対象 →
```

つまり、日本語の指示と、それまでに出力したコードを読んで、正しい次のコードトークンを予測できるかだけでlossを計算する。実装上は、損失を計算しない位置のラベルを、PyTorchで一般的に使われる`-100`に設定すればよい。もし、指示部分にも損失を計算すると、モデルは日本語の問題文そのものを予測する機能とコーディング機能の両方を学習してしまう。一方、コード部分だけに損失を計算すると、学習能力を主に後者へ集中させることができる。

なお、指示部分の損失を無視しても、モデルが指示を読まなくなるわけではない。トークンとしては正しく内部表現に変換され、学習される。「問題部分の予測をしなくなる」という点が重要である。

### 全トークンを計算対象にする場合

日本語教材やPython解説文など、文章全体を言語モデルとして学習させたいデータでは、全トークンを対象にすると良い。

```text
Pythonのリスト内包表記は、リストを簡潔に作る構文です。例えば……
← 全体を損失計算の対象にする →
```

これはモデルに日本語、Pythonの語彙、説明文の構造などを基礎から学ばせるためです。

したがって、課題文の該当箇所は次のように言い換えると、より分かりやすくなります。

## 評価指標

モデルの評価では、BLEUや文字列一致よりも実行結果を重視する。

| 指標              | 内容                                   |
| ----------------- | -------------------------------------- |
| Validation loss   | 学習が進んだか                         |
| Syntax-valid率    | `ast.parse`に成功した割合              |
| Safe-AST率        | 禁止構文を含まない割合                 |
| Signature-valid率 | `solve(xs, k)`を正しく定義した割合     |
| Executable率      | timeoutや例外なしで実行できた割合      |
| pass@1            | 1回の生成で全hidden testに合格した割合 |
| pass@5            | 5候補のうち1候補以上が合格した割合     |
| 組合せ汎化率      | 未学習の演算組合せに対するpass@1       |
| 最大GPUメモリ     | 実行可能性の記録                       |
| tokens/s          | 再現性と計算効率の記録                 |

テスト問題ごとに20個以上のランダム入力を生成する。コード作成時に使用したテストとは異なるhidden testで判定する。

## 比較実験

最低一つ、可能なら二つの比較実験を行う。

推奨候補：

- 学習前のランダムモデル対学習後モデル
- 10M tokens対100M tokens
- 教師言い換えなし対あり
- コード表現1種類対複数種類
- 30Mモデル対70Mモデル
- 重複除去前対重複除去後
- ランダム分割対意味AST分割

比較では一度に一つの条件だけを変更する。

## ゴールとして確認すべきこと

本課題の完了条件は次のとおりである。

- データ生成から評価まで、一つの手順で再実行できる
- トークナイザとモデルがランダム初期値から作成されている
- 訓練lossと検証lossの推移を提示できる
- 学習前後の出力を比較できる
- 教師モデルなしで学生モデル単独の推論ができる
- 未学習の意味ASTに対して100問以上評価している
- モデルが生成したコードのうち、少なくとも5問がhidden testを完全通過する
- 成功例だけでなく、最低20件の失敗を分類している
- データ漏洩検査の結果を報告している
- 使用データ、モデル、ソフトウェアの由来を記録している

目安として、Syntax-valid率80%以上、通常テストのpass@1 30%以上を健康状態の参考値とする。ただし、この数値そのものを成績評価の中心にはしない。

達しなかった場合も、原因を切り分ける比較実験が十分であれば評価対象とする。ただし、最終デモまでには少数でも実行成功例を得ること。

## 学ぶ内容

本課題を通じて、学生は次を学ぶ。

- 自己回帰言語モデルの学習原理
- トークナイザと語彙設計
- Transformerのモデルサイズと計算量
- teacher inferenceとstudent trainingの違い
- response-based distillationの考え方
- 合成データにおける品質と多様性の関係
- train/test leakageの危険性
- 実行結果によるコード評価
- データ重複と難易度分布
- BF16、Gradient Accumulation、Checkpointing
- GPUメモリと学習速度の測定
- 再現可能な機械学習実験
- モデル、データ、ソフトウェアの由来管理
- 失敗例から仮説を作る研究方法

## 発展課題：Boku1 1B

本課題を完了した時点で、諸君は「Boku1 1B」の予備実験を終えた状態にある。以下は発展の方向であり、卒業研究のテーマになり得る。

約1Bパラメータの例：

| 項目             |    値 |
| ---------------- | ----: |
| 層数             |    22 |
| hidden size      | 2,048 |
| Attention head数 |    16 |
| KV head数        |     4 |
| FFN size         | 5,504 |
| 語彙数           | 8,192 |
| 最大系列長       |   256 |

このモデルもランダム初期値から学習する。ただし、夏休み中に十分な事前学習を完了させることは要求しない。

推奨する範囲は次のとおり。

- まず10M tokensの動作確認
- 次に50〜200M tokensの短期学習
- Boku1-nanoとloss、生成例、速度を比較
- 学習不足の1Bモデルが小型モデルより良いとは限らないことを確認

## ライセンスと来歴管理

Apache License 2.0のQwen3公式モデルは教師候補として扱いやすいが、モデルのライセンスだけで、すべての生成物について第三者の権利が存在しないことまで保証されるわけではない。そこで、以下のような情報を記録しておく。

- 使用したモデルのLICENSEを保存する
- 正確なモデルrevisionを固定する
- 教師プロンプトと生成条件を保存する
- インターネットから取得したコードを合成データへ混入させない
- 既存コードとの長い一致を検査する
- 自作テンプレートとルール生成物を中心にする
- `THIRD_PARTY.md`を作成する
- Dataset CardとModel Cardに生成方法を明記する

## 研究課題への発展

本実習から、次の独立した研究テーマへ発展できる。

- どの構造変換が組合せ汎化を改善するか
- 日本語言い換えの多様性と実行精度の因果関係
- 意味重複を残す場合と除去する場合の比較
- 難易度カリキュラムの効果
- 合成コードの表面的多様性と意味的多様性の違い
- 教師モデルサイズと学生性能の関係
- 実行フィードバックを用いた反復的データ改善
- 失敗例を重点的に追加するactive data generation
- 30M、70M、300M、1B間の小規模スケーリング則

## 付録: 参照すべき文献・資料

- Vaswani et al., _Attention Is All You Need_ (2017)
- Hoffmann et al., _Training Compute-Optimal Large Language Models_ (2022)
- Kaplan et al., _Scaling Laws for Neural Language Models_ (2020)
- Su et al., _RoFormer: Enhanced Transformer with Rotary Position Embedding_ (2021)
- Shazeer, _GLU Variants Improve Transformer_ (2020)
- Zhang & Sennrich, _Root Mean Square Layer Normalization_ (2019)
- Chen et al., _Evaluating Large Language Models Trained on Code_ (2021)
- Apache License, Version 2.0 本文
